import { GaleriaActivePipe } from './galeria-active.pipe';

describe('GaleriaActivePipe', () => {
  it('create an instance', () => {
    const pipe = new GaleriaActivePipe();
    expect(pipe).toBeTruthy();
  });
});
